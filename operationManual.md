# 我们的故事 · 操作手册

> 本手册教你如何为这个情侣日记网站添加内容。所有操作都是**编辑本地文件**,无需数据库,无需登录后台。

---

## 📑 目录

1. [开始前的准备](#1-开始前的准备)
2. [写一篇日记](#2-写一篇日记)
3. [相册上传与分类](#3-相册上传与分类)
4. [管理 Others 小玩意](#4-管理-others-小玩意)
5. [图片资源管理](#5-图片资源管理)
6. [本地预览与发布上线](#6-本地预览与发布上线)
7. [常见问题](#7-常见问题)

---

## 1. 开始前的准备

### 1.1 你需要用到的工具

| 工具 | 用途 | 推荐 |
|------|------|------|
| **代码编辑器** | 编辑文件 | VS Code（免费，最好用） |
| **图片** | 放在 `public/images/` | 自己拍的照片 |
| **终端** | 预览、发布 | VS Code 自带的终端即可 |

### 1.2 认识项目结构

```
项目根目录/
├── posts/              ← 📝 你的日记放这里（每个 .md 文件是一篇）
├── data/
│   ├── gallery.json    ← 📸 相册照片数据
│   └── others.json     ← 🎁 小玩意数据
├── public/images/      ← 🖼️ 所有图片放这里
├── app/                ⚙️ 网页代码（一般不用改）
├── components/         ⚙️ 组件代码（一般不用改）
└── out/                📦 构建产物（自动生成，别手动改）
```

> 💡 **简单记法**：你日常只需要碰 3 个地方 → `posts/`（日记）、`data/`（相册+小玩意）、`public/images/`（图片）。

### 1.3 启动本地预览（边写边看效果）

在项目根目录打开终端，运行：

```bash
npm run dev
```

然后浏览器打开 **http://localhost:3000**，你每次保存文件，页面会自动刷新。

---

## 2. 写一篇日记

### 2.1 日记存在哪里？

日记就是 `posts/` 文件夹里的一个 `.md`（Markdown）文件，**一个文件 = 一篇日记**。

### 2.2 创建一篇新日记（3 步）

#### 第 1 步：在 `posts/` 文件夹新建文件

文件名建议用**英文 + 日期或主题**，例如 `2026-08-19-bicycle.md`。

> ⚠️ 文件名会成为网址的一部分（`/diary/2026-08-19-bicycle`），所以建议用英文、数字、连字符，**避免中文和空格**。

#### 第 2 步：写入内容

打开文件，粘贴下面的模板，把内容改成你的：

```markdown
---
title: "一起骑车去郊外"
date: "2026-08-19"
tags: ["日常", "运动"]
cover: "/images/posts/cycling.jpg"
excerpt: "秋天的风刚刚好，我们骑到了一片没人知道的向日葵花海。"
---

今天天气特别好，你提议骑车去郊外。

## 出发

早上八点，我们背上水壶和零食出发了。沿途的稻田已经开始转黄，风里有收获的味道。

## 意外发现

骑了大概十公里，我们拐进一条小路，眼前突然出现一大片向日葵！

![向日葵花海](/images/posts/sunflower.jpg)

你冲进去就开始拍照，我在旁边看着你笑。

## 写在最后

回家路上你说："以后每周末都骑车吧。" 我说好。

> 原来幸福就是，和你一起骑向一个未知的小路。
```

#### 第 3 步：保存

按 `Ctrl + S` 保存。打开 http://localhost:3000/diary 就能看到新日记了。

### 2.3 Frontmatter 字段详解（顶部那一段 `---` 之间的内容）

| 字段 | 必填 | 说明 | 示例 |
|------|------|------|------|
| `title` | ✅ | 日记标题 | `"一起骑车去郊外"` |
| `date` | ✅ | 发布日期，决定排序（新的在前） | `"2026-08-19"` |
| `tags` | 可选 | 标签，方便分类 | `["日常", "运动"]` |
| `cover` | 可选 | 封面图路径（见第 5 节） | `"/images/posts/cycling.jpg"` |
| `excerpt` | 可选 | 摘要（列表卡片上显示）。不写会自动截取正文前 100 字 | `"秋天的风刚刚好……"` |

### 2.4 Markdown 语法速查（写正文用）

| 你想要的效果 | 写法 | 效果 |
|------|------|------|
| 大标题 | `## 标题` | 章节标题 |
| 段落 | 直接写文字 | 普通段落 |
| **加粗** | `**文字**` | **文字** |
| *斜体* | `*文字*` | *文字* |
| 插入图片 | `![描述](/images/xxx.jpg)` | 图片居中显示 |
| 引用 | `> 这是一句话` | 左侧带粉色竖线 |
| 列表 | `- 第一项` | 圆点列表 |
| 有序列表 | `1. 第一项` | 数字列表 |
| 分割线 | `---` | 虚线分隔 |

### 2.5 删除一篇日记

直接在 `posts/` 文件夹**删除对应的 `.md 文件**即可。下次构建时它就不再出现。

### 2.6 修改一篇日记

直接打开 `.md` 文件改内容，保存即可。

---

## 3. 相册上传与分类

### 3.1 相册数据存在哪里？

相册的所有照片信息都在 **`data/gallery.json`** 这一个文件里。它是一个 JSON 数组，每张照片是一项。

### 3.2 添加一张照片（3 步）

#### 第 1 步：把照片文件放进图片目录

把你的照片复制到 `public/images/gallery/` 文件夹，例如命名为 `park.jpg`。

#### 第 2 步：编辑 `data/gallery.json`

打开 `data/gallery.json`，在 `photos` 数组里**复制一份**现有照片，改成你的新照片：

```json
{
  "photos": [
    {
      "id": "1",
      "title": "西湖落日",
      "date": "2026-07-20",
      "description": "夕阳下的断桥，我们的影子拉得很长。",
      "imageUrl": "/images/gallery/west-lake.jpg",
      "category": "旅行"
    },
    {
      "id": "2",
      "title": "周末野餐",
      "date": "2026-08-19",
      "description": "草坪上铺开格子布，你切水果我发呆。",
      "imageUrl": "/images/gallery/park.jpg",
      "category": "日常"
    }
  ]
}
```

#### 第 3 步：保存

保存文件，刷新 http://localhost:3000/gallery ，新照片就出现了，点击还能打开灯箱看大图。

### 3.3 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | ✅ | 唯一编号，建议用数字递增（1、2、3…），不能重复 |
| `title` | ✅ | 照片标题（灯箱里显示） |
| `date` | ✅ | 拍摄日期，格式 `YYYY-MM-DD` |
| `description` | 可选 | 照片描述（灯箱里显示） |
| `imageUrl` | ✅ | 照片路径，**必须以 `/images/gallery/` 开头** |
| `category` | ✅ | 分类，用于顶部筛选标签（见下文） |

### 3.4 相册分类是怎么来的？（重点）

**你不需要预先定义分类！** 网页会自动收集所有照片的 `category` 字段，生成顶部的筛选标签。

例如你的照片分类分别是：

- `"旅行"` → 标签出现「旅行」
- `"日常"` → 标签出现「日常」
- `"纪念日"` → 标签出现「纪念日」

筛选栏会自动变成：**全部 | 旅行 | 日常 | 纪念日**

> 💡 **想加新分类？** 只需在某张照片写上新的 `category` 值，比如 `"节日"`，筛选标签会自动多出来一个「节日」。

### 3.5 删除一张照片

在 `data/gallery.json` 里**删掉那一整项**（从 `{` 到对应的 `}`，包括末尾的逗号）。建议同时删掉 `public/images/gallery/` 里对应的图片文件，节省空间。

### 3.6 ⚠️ JSON 格式注意事项

JSON 格式很严格，容易出错：

✅ **正确**：每项之间用逗号 `,` 分隔
```json
{ "id": "1", ... },
{ "id": "2", ... }
```

❌ **错误**：最后一项多了逗号
```json
{ "id": "1", ... },
{ "id": "2", ... },   ← 最后一个不能有逗号！
```

✅ 双引号必须用英文 `"`，不能用中文 `"` `"`

✅ 改完保存后，如果页面打不开，多半是 JSON 写错了，检查逗号和引号。

---

## 4. 管理 Others 小玩意

### 4.1 数据存在哪里？

`data/others.json`，结构和相册类似。

### 4.2 添加一个新的小玩意

```json
{
  "items": [
    {
      "id": "qixi",
      "title": "💗 七夕心动实验室",
      "description": "爱心漂浮，星光点点，为你制造一个浪漫夜晚",
      "cover": "/images/others/qixi-cover.jpg",
      "slug": "qixi",
      "tags": ["情感", "节日"]
    },
    {
      "id": "memory-wall",
      "title": "📸 我们的回忆墙",
      "description": "把每一个心动的瞬间都钉在墙上",
      "cover": "/images/others/memory-wall.jpg",
      "slug": "memory-wall",
      "tags": ["情感", "互动"]
    }
  ]
}
```

### 4.3 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | ✅ | 唯一编号，英文 |
| `title` | ✅ | 卡片标题 |
| `description` | ✅ | 卡片描述 |
| `cover` | ✅ | 封面图路径 |
| `slug` | ✅ | 网址路径名，决定详情页地址（见下） |
| `tags` | 可选 | 标签 |

### 4.4 关于详情页

- `slug` 为 `qixi` 的，会进入**七夕专属浪漫交互页**（已有代码）
- `slug` 为其他值的（如 `memory-wall`），会进入**通用详情页**（显示封面+描述）
- 想给某个小玩意做**专属交互页**，需要让懂代码的人改 `app/others/[slug]/page.js`，普通使用建议先用通用页。

### 4.5 删除小玩意

在 `data/others.json` 删掉对应项即可。

---

## 5. 图片资源管理

### 5.1 图片放在哪里？

所有图片统一放在 `public/images/` 下，按用途分文件夹：

```
public/images/
├── posts/      ← 日记配图
├── gallery/    ← 相册照片
└── others/     ← 小玩意封面
```

### 5.2 在代码里怎么引用？

图片路径**永远以 `/images/` 开头**（代表 `public/` 文件夹）：

| 物理位置 | 代码里写 |
|------|------|
| `public/images/posts/cycling.jpg` | `/images/posts/cycling.jpg` |
| `public/images/gallery/park.jpg` | `/images/gallery/park.jpg` |
| `public/images/others/qixi.jpg` | `/images/others/qixi.jpg` |

### 5.3 图片建议

- **格式**：`.jpg`（照片）、`.png`（带透明）、`.webp`（更小，推荐）
- **尺寸**：
  - 日记封面：宽 1200px 左右
  - 日记正文图：宽 800~1000px
  - 相册照片：正方形或接近正方形（会被裁剪成方形显示）
- **文件名**：用英文，如 `sunset-2026.jpg`，**避免中文和空格**

### 5.4 用在线占位图（不想传图片时）

如果暂时没有合适图片，可以用占位图服务：

```
https://picsum.photos/seed/任意英文/宽度/高度
```

例如封面：`https://picsum.photos/seed/sunset/1200/600`

### 5.5 修改首页 Hero 区的"我们的故事"配图

首页的两张简介图在 `app/page.js` 里，搜索 `picsum.photos/seed/story1` 和 `story2`，把链接换成你的图片路径即可。

---

## 6. 本地预览与发布上线

### 6.1 写完后预览（开发模式）

```bash
npm run dev
```
- 实时刷新，适合边写边看
- 访问 http://localhost:3000

### 6.2 预览生产效果（和上线一模一样）

```bash
npm run preview
```
- 会先构建再启动本地服务器
- 适合发布前最终检查

### 6.3 发布上线（自动方式，推荐）

本项目已配置 GitHub Actions 自动部署。你只需要：

```bash
git add .
git commit -m "新增日记：一起骑车去郊外"
git push
```

推送后，GitHub 会**自动构建并发布**到 https://djl2lq.github.io/ ，约 1~2 分钟后刷新即可看到更新。

> 💡 第一次部署前，需要确认 GitHub 仓库设置：
> **Settings → Pages → Source 选择 "GitHub Actions"**（一次性设置）

### 6.4 发布上线（手动方式，备用）

如果自动部署出问题，可手动发布：

```bash
npm run deploy
```

这会把 `out/` 目录推送到 `gh-pages` 分支。**注意：此方式需要在 GitHub Pages 设置里把 Source 选为 `gh-pages` 分支**，与自动方式二选一。

### 6.5 修改页脚的"相恋天数"

打开 `components/Footer.js`，找到这一行：

```js
const START_DATE = '2024-05-20';
```

把日期改成你们在一起的那天，格式 `YYYY-MM-DD`，页脚会自动显示相恋天数。

---

## 7. 常见问题

### Q1：保存后页面没变化？
- 确认保存了文件（`Ctrl + S`）
- `npm run dev` 模式下刷新浏览器
- 检查终端有没有红色报错

### Q2：日记详情页打不开 / 报错？
- 检查 `.md` 文件顶部 `---` 之间的格式，`title` 和 `date` 不能少
- `date` 必须是 `"YYYY-MM-DD"` 格式
- 文件名不要用中文或空格

### Q3：相册页面白屏 / 报错？
- 99% 是 `data/gallery.json` 格式错了
- 检查：逗号、引号、括号是否配对
- 用 [JSON 校验工具](https://jsonlint.com/) 验证一下

### Q4：图片显示不出来？
- 确认图片文件在 `public/images/` 对应文件夹下
- 路径必须以 `/images/` 开头，例如 `/images/gallery/park.jpg`
- 文件名大小写要完全一致（`Park.jpg` ≠ `park.jpg`）

### Q5：相册想加新分类？
- 不需要任何额外操作，直接在某张照片的 `category` 写上新分类名，筛选标签会自动出现

### Q6：怎么调整日记排序？
- 排序按 `date` 字段，**日期越新越靠前**。改日期即可调整顺序。

### Q7：想把日记设为草稿（暂不显示）？
- 暂时把 `.md` 文件移出 `posts/` 文件夹（比如放到桌面），或者改文件名加 `_` 前缀（如 `_draft.md`）就不会被读取。

### Q8：推送后线上没更新？
- 进 GitHub 仓库 → **Actions** 标签，看部署 workflow 是否绿色对勾
- 若失败，点进去看日志
- 确认 Pages 设置里 Source 是 "GitHub Actions"

---

## ✅ 日常操作速查表

| 我想… | 我要改… |
|------|------|
| 写新日记 | 在 `posts/` 新建 `.md` 文件 |
| 改日记 | 编辑 `posts/` 里对应 `.md` |
| 删日记 | 删掉 `posts/` 里对应 `.md` |
| 加相册照片 | 编辑 `data/gallery.json` + 放图到 `public/images/gallery/` |
| 加小玩意 | 编辑 `data/others.json` + 放图到 `public/images/others/` |
| 改相恋天数 | 改 `components/Footer.js` 里的 `START_DATE` |
| 改首页简介图 | 改 `app/page.js` 里的图片链接 |
| 发布 | `git add .` → `git commit -m "说明"` → `git push` |

---

需要更多功能或有疑问，随时查阅 [README.md](./README.md) 或 [version.md](./version.md)。
